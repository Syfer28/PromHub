import {
  View,
  Text,
  TextInput,
  ScrollView,
  Pressable,
  StyleSheet,
} from "react-native";

const SearchBar = () => {
  retunr(
    <View>
      <TextInput placeholder="hello" />
    </View>
  );
};

export default SearchBar;
